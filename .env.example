# Set this in flutter run/build via --dart-define instead of .env file.
ALPHA_VANTAGE_PROXY_URL=https://your-domain.com/query
# Optional fallback: used only when proxy fails/rate-limited.
ALPHA_VANTAGE_API_KEY=YOUR_ALPHA_VANTAGE_KEY
